{
	"name": "@diffagent/devhub",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Parallel dev environment orchestrator (pool of worktrees, Docker containers, Traefik, Cloudflare tunnel). Currently Diluu-specific; extracted from Diluu so it can live next to DiffAgent.",
	"bin": {
		"devhub": "src/index.ts"
	},
	"scripts": {
		"start": "tsx src/index.ts",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"tsx": "^4.0.0"
	},
	"devDependencies": {
		"typescript": "^6.0.2"
	}
}
